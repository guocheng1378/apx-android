# L3 · Android 副屏与 App UI（开工回报）

- laneId：`L3` ｜ agent：`android-screen-ui`
- 状态：**IN_PROGRESS**（开工回报，最终回报会覆盖本文件）
- 时间：2026-09-20T12:21:51+08:00

## 归属目录

- `android/app/src/main/java/com/allperiph/screen/`
- `android/app/src/main/java/com/allperiph/ui/`
- `android/app/src/main/res/`
- `android/app/src/main/AndroidManifest.xml`

## 上一轮已有产出（盘点结果）

已有：`screen/protocol/{ApxProtocol,FrameReader,VideoFrameHeader}.kt`、`screen/transport/{BulkTransport,JniBulkTransport,TcpBulkTransport,Transports}.kt`、`res/`（layout/values/drawable）、`AndroidManifest.xml`。
缺失：`screen/` 的解码器/渲染/触控采集/模块编排，`ui/` 目录**完全为空**（Manifest 已声明但未实现）。

## 本轮计划

1. `screen/protocol/`：触控载荷（§2.5/§3.2）与 APX 帧写出器
2. `screen/`：MediaCodec 低延迟解码 + SurfaceView 渲染 + 丢帧策略 + 延迟统计 + 触控上行
3. `ui/`：主界面、副屏 Activity、前台服务、通知渠道、环境检测与引导、服务编排
4. `res/` + `AndroidManifest.xml` 收口（保持零第三方依赖，仅用 framework API）
5. 各目录 README.md，回报文件收尾并运行 `python scripts/aggregate.py` 自查

## 验证

- `python scripts/aggregate.py`（汇总器，真实执行）
- 静态一致性检查（XML 合法性、资源引用闭合、包路径匹配）——本机无 JDK/Android SDK/Gradle，**无法执行真实编译**，编译项一律 `passed:false` 并说明。
