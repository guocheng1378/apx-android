# L4 · PC 副屏驱动与推流（pc-display）

- laneId：`L4`　agent：`pc-display`　归属目录：`pc/display/`
- 状态：`IN_PROGRESS`（随子模块推进增量更新，最终改为 `PARTIAL`/`COMPLETED`）

## 1. 本路职责（docs/ARCHITECTURE.md §5）

下行：IddCx 虚拟显示器 → Desktop Duplication 抓屏 → 只编码 DirtyRects → H.265/AV1 硬编（关 B 帧、GOP=1）→ bulk 推流 → 手机解码渲染。
上行：手机触控 → bulk → PC 注入（三档递进）。

## 2. 当前进展

| 子模块 | 目录 | 状态 |
|---|---|---|
| IddCx 虚拟显示器驱动 | `pc/display/idd/` | 进行中 |
| DDA 抓屏（DirtyRects/MoveRects） | `pc/display/capture/` | 进行中 |
| 硬编抽象（NVENC/QSV/AMF/软编回退） | `pc/display/encode/` | 进行中 |
| 组帧 + USB 传输 + 控制面/心跳/RTT | `pc/display/transport/` | 进行中 |
| 触控注入三档 | `pc/display/inject/` | 进行中 |
| 编排与 CLI | `pc/display/pipeline/`、`pc/display/app/` | 进行中 |

## 3. 已知约束

- 本机无 WDK → 驱动 `built = no`，验证项 `passed = false` 并注明原因（不静默）。
- `shared/`（L1）尚未产出 → 按 `docs/PROTOCOL.md` 直写，缺接口记入 `contractDeviations`（`neededFrom = L1`）。

## 4. 回报文件

- 机读：`reports/L4.json`
- 人读：本文件 `reports/L4.md`
